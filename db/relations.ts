// Relations are handled via foreign key references in the schema
// SQLite foreign key support is used for referential integrity
// Query-time relations can be added here if needed for Drizzle's relational API

export {};
